import type { SyncHandlerInterface } from '@livy/contracts';
/**
 * Check whether a value implements the SyncHandlerInterface
 *
 * @param value The value to check
 */
export declare function isSyncHandlerInterface(value: unknown): value is SyncHandlerInterface;
